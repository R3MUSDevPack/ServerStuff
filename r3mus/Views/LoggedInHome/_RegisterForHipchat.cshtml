@using (Html.BeginForm("RegisterForHipchat", "LoggedInHome", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <fieldset>
        <input type="submit" value="Register" class="btn btn-default" />
    </fieldset>
}
